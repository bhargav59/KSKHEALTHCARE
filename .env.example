# Cloudflare Workers API Configuration
# Copy this file to .env and fill in your values

# Cloudflare Workers API URL
# After deploying with 'npx wrangler deploy', you'll get a URL like:
# https://kskhealthcare-api.your-subdomain.workers.dev
VITE_API_URL=https://kskhealthcare-api.your-subdomain.workers.dev

# Admin Email (for admin access)
VITE_ADMIN_EMAIL=admin@kskhealthcare.in

# WhatsApp Auto-Send Numbers
VITE_WHATSAPP_NUMBER=919900546089
VITE_WHATSAPP_BUSINESS_NUMBER=918884333439

# ==================== FIREBASE CONFIGURATION ====================
# Add the Firebase credentials for your project below. Create a `.env` file
# by copying this file and updating the values before running the app.
# You can find these credentials in your Firebase Console → Project settings → General → Your apps
VITE_FIREBASE_API_KEY=AIzaSy...                             # example only - replace with your key
VITE_FIREBASE_AUTH_DOMAIN=ksk-healthcare.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=ksk-healthcare
VITE_FIREBASE_STORAGE_BUCKET=ksk-healthcare.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abc123
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXX

# ==================== CLOUDFLARE SETUP ====================
# These are configured in wrangler.toml, not .env:
# 
# 1. JWT_SECRET - Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 2. KV Namespace IDs - Created with: npx wrangler kv namespace create "APPOINTMENTS"
# 3. ADMIN_EMAIL - Set in wrangler.toml vars section
#
# See CLOUDFLARE_SETUP_GUIDE.md for complete setup instructions

