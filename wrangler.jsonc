{
  // Cloudflare Worker/Assets configuration for deploying the static Vite build
  "name": "kskhealthcare1",
  "compatibility_date": "2025-08-09",
  // Run the Vite production build before deploying
  "build": {
    "command": "pnpm run build"
  },
  // Deploy the built static assets from the dist directory
  "assets": {
    "directory": "./dist"
  }
}
